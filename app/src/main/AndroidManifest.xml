<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.bookers">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Homepage" />
        <activity android:name=".login" />
        <activity android:name=".Add_books" />
        <activity android:name=".newsignup" />
        <activity android:name=".Search_Result" />
        <activity android:name=".My_Collection" />
        <activity android:name=".Details_books" />
        <activity android:name=".My_Rents" />
        <activity android:name=".RENTLIST"></activity>
        <activity android:name=".Details_books_MC">
        </activity>
    </application>

</manifest>